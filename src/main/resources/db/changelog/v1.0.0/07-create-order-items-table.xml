<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="07-create-order-items-table" author="developer">
        <comment>Создание таблицы элементов заказа</comment>

        <createTable tableName="order_items">
            <!-- Базовые поля -->
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITHOUT TIME ZONE">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITHOUT TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <!-- Поля элемента заказа -->
            <column name="order_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="product_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="product_name" type="VARCHAR(200)">
                <constraints nullable="false"/>
            </column>
            <column name="quantity" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="price" type="DECIMAL(10,2)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Создание внешних ключей -->
        <addForeignKeyConstraint
                baseTableName="order_items"
                baseColumnNames="order_id"
                constraintName="fk_order_items_order"
                referencedTableName="orders"
                referencedColumnNames="id"
                onDelete="CASCADE"
                onUpdate="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="order_items"
                baseColumnNames="product_id"
                constraintName="fk_order_items_product"
                referencedTableName="products"
                referencedColumnNames="id"
                onDelete="RESTRICT"
                onUpdate="CASCADE"/>

        <!-- Добавляем комментарии -->
        <sql>
            COMMENT ON TABLE order_items IS 'Элементы заказа (товары в заказе с историческими данными)';
            COMMENT ON COLUMN order_items.order_id IS 'ID заказа';
            COMMENT ON COLUMN order_items.product_id IS 'ID товара';
            COMMENT ON COLUMN order_items.product_name IS 'Название товара на момент заказа (снимок)';
            COMMENT ON COLUMN order_items.quantity IS 'Количество товара в заказе';
            COMMENT ON COLUMN order_items.price IS 'Цена товара на момент заказа';
        </sql>

        <!-- Проверочные ограничения -->
        <sql>
            ALTER TABLE order_items ADD CONSTRAINT chk_order_items_quantity_positive
                CHECK (quantity > 0);

            ALTER TABLE order_items ADD CONSTRAINT chk_order_items_price_positive
                CHECK (price > 0);

            ALTER TABLE order_items ADD CONSTRAINT chk_order_items_product_name_not_empty
                CHECK (LENGTH(TRIM(product_name)) > 0);
        </sql>
    </changeSet>

</databaseChangeLog>