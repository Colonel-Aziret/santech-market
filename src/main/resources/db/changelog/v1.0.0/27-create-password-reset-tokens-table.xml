<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="27-create-password-reset-tokens-table" author="system">
        <comment>Создание таблицы для токенов сброса пароля</comment>

        <createTable tableName="password_reset_tokens">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="token" type="VARCHAR(6)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"
                             foreignKeyName="fk_password_reset_tokens_user"
                             references="users(id)"
                             deleteCascade="true"/>
            </column>

            <column name="expiry_date" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="is_used" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Индексы для оптимизации запросов -->
        <createIndex indexName="idx_password_reset_token" tableName="password_reset_tokens">
            <column name="token"/>
        </createIndex>

        <createIndex indexName="idx_password_reset_user_id" tableName="password_reset_tokens">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_password_reset_expiry" tableName="password_reset_tokens">
            <column name="expiry_date"/>
        </createIndex>

        <createIndex indexName="idx_password_reset_is_used" tableName="password_reset_tokens">
            <column name="is_used"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
