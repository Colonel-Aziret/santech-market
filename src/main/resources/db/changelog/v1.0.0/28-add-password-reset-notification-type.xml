<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="28-add-password-reset-notification-type" author="system">
        <comment>Добавление типа PASSWORD_RESET в ограничение проверки notifications</comment>

        <!-- Удаляем старый констрейнт -->
        <sql>
            ALTER TABLE notifications DROP CONSTRAINT IF EXISTS chk_notifications_type;
        </sql>

        <!-- Создаем новый констрейнт с добавленным PASSWORD_RESET -->
        <sql>
            ALTER TABLE notifications
            ADD CONSTRAINT chk_notifications_type
            CHECK (type IN ('DISCOUNT', 'ANNOUNCEMENT', 'ORDER_UPDATE', 'NEW_PRODUCT', 'SYSTEM', 'PASSWORD_RESET'));
        </sql>
    </changeSet>

</databaseChangeLog>
